import Ember from 'ember';

export default Ember.Route.extend({

    // model: function() {
    //     return Class.create({
    //         name: "Tom Dale"
    //     });
    // },

    actions: {
        addNewClass() {
            // console.log('submit');
            // let newclass = this.get('model')();
            // newclass.set(description, 'new val');
            // console.log(newclass.description);
            //console.log(this.get('title'));
        }
    }
});
